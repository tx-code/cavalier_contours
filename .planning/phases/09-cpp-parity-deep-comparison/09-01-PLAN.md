---
phase: 09
plan: 09-01
type: implementation
wave: 1
depends_on: []
autonomous: true
requirements:
  - PAR-01
  - PAR-02
  - PAR-03
files_modified:
  - .planning/phases/09-cpp-parity-deep-comparison/09-CPP-BOOLEAN-PARITY.md
  - cavalier_contours/tests/test_cpp_combine_parity.rs
must_haves:
  truths:
    - "Promote known metadata-only C++ combine gap to executable Rust parity checks."
    - "Classify observed mismatches as bug/divergence/not-comparable with evidence."
---

# Plan 09-01: Boolean/Combine Deep Parity Start

<objective>
Create executable parity tests for high-value C++ combine cases and produce the
first evidence-backed mismatch classification.
</objective>

<tasks>

<task>
<name>Create executable C++ combine parity tests</name>
<files>
- `cavalier_contours/tests/test_cpp_combine_parity.rs`
</files>
<action>
Add tests for C++ simple circle/rectangle combine cases (`union`, `exclude`,
`intersect`, `xor`) using C++ expected properties as parity baseline. Record
topology deltas explicitly when geometry properties match but vertex topology
differs.
</action>
<verify>
`cargo test -p cavalier_contours --test test_cpp_combine_parity -- --nocapture`
</verify>
</task>

<task>
<name>Write first boolean parity classification report</name>
<files>
- `.planning/phases/09-cpp-parity-deep-comparison/09-CPP-BOOLEAN-PARITY.md`
</files>
<action>
Document module mapping, executed case outcomes, and mismatch classification.
</action>
<verify>
`Select-String -Path .planning\phases\09-cpp-parity-deep-comparison\09-CPP-BOOLEAN-PARITY.md -Pattern "bug","intentional-divergence","not-comparable"`
</verify>
</task>

</tasks>
