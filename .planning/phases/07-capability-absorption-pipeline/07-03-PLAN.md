---
phase: 07
plan: 07-03
type: implementation
wave: 3
depends_on:
  - 07-01
  - 07-02
autonomous: true
requirements:
  - CAP-02
  - CAP-03
files_modified:
  - .planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md
  - cavalier_contours/src/polyline/traits.rs
  - cavalier_contours/src/polyline/internal/
  - cavalier_contours/src/shape_algorithms/mod.rs
  - cavalier_contours/tests/
must_haves:
  truths:
    - "D-03: Implement only one first capability slice."
    - "D-06: Triangulation, production Clipper2 backend work, broad generated corpus import, and new offset join-style families stay out of this phase."
    - "D-08: Every absorbed capability needs focused tests first or with implementation."
    - "D-09: Public API and FFI impacts must be documented when visible."
---

# Plan 07-03: Implement First Absorbed Capability Slice

<objective>
Implement the selected first capability slice from `07-CAPABILITY-DESIGN.md`
with focused tests and no unplanned public surface drift.
</objective>

<context>
@.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md
@.planning/phases/07-capability-absorption-pipeline/07-PATTERNS.md
@cavalier_contours/tests/test_utils/fixture_schema.rs
@cavalier_contours/tests/test_utils/fixture_harness.rs
@cavalier_contours/src/polyline/traits.rs
@cavalier_contours/src/shape_algorithms/mod.rs
</context>

<tasks>

<task>
<name>Add focused capability tests</name>
<read_first>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
- `.planning/phases/07-capability-absorption-pipeline/07-PATTERNS.md`
- `cavalier_contours/tests/test_utils/fixture_schema.rs`
- `cavalier_contours/tests/test_utils/fixture_harness.rs`
- The existing test file named by `Test files` in `07-CAPABILITY-DESIGN.md`
</read_first>
<files>
- The test file named by `Test files` in `07-CAPABILITY-DESIGN.md`
</files>
<action>
Add focused tests for the selected capability. Use the exact selected candidate
ID and provenance from `07-CAPABILITY-DESIGN.md`. Prefer property comparison
for geometry results. Include a negative or boundary case when the candidate
involves tolerance, open/closed behavior, degenerates, or polygon-only
classification.
</action>
<acceptance_criteria>
- The selected candidate ID from `07-CAPABILITY-DESIGN.md` appears in the test
  file.
- The targeted test command from `07-CAPABILITY-DESIGN.md` exits 0.
- The test does not require a live Clipper2 build or external repository at
  normal test time.
</acceptance_criteria>
<verify>
Run the `Targeted test command` recorded in `07-CAPABILITY-DESIGN.md`.
</verify>
</task>

<task>
<name>Implement selected capability</name>
<read_first>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
- Each file listed under `Implementation files` in `07-CAPABILITY-DESIGN.md`
- `cavalier_contours/src/lib.rs`
- `cavalier_contours/src/polyline/traits.rs`
- `cavalier_contours/src/shape_algorithms/mod.rs`
</read_first>
<files>
- Each production file listed under `Implementation files` in `07-CAPABILITY-DESIGN.md`
</files>
<action>
Implement only the selected first slice. Keep the core crate safe Rust, avoid
adding a production Clipper2 dependency, and keep changes inside the files named
by the design unless the implementation discovers a necessary adjacent helper.
If an adjacent helper is needed, update `07-CAPABILITY-DESIGN.md` before coding
it so the impact remains recorded.
</action>
<acceptance_criteria>
- `cargo test -p cavalier_contours --test <selected_test>` exits 0 using the
  selected test name from `07-CAPABILITY-DESIGN.md`.
- `rg "unsafe" cavalier_contours/src` reports no new unsafe core code.
- `git diff --name-only` does not include `cavalier_contours_ffi.h` unless
  `Generated header impact` in `07-CAPABILITY-DESIGN.md` is not `none`.
</acceptance_criteria>
<verify>
Run the targeted test command from `07-CAPABILITY-DESIGN.md`, then `git diff --check`.
</verify>
</task>

</tasks>

<verification>

- Targeted test command from `07-CAPABILITY-DESIGN.md`
- `cargo fmt --all --check`
- `cargo clippy --all-targets -- -D warnings`
- `git diff --check`

</verification>

<output>
After completion, create `.planning/phases/07-capability-absorption-pipeline/07-03-SUMMARY.md`.
</output>

