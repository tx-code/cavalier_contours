---
phase: 07
plan: 07-04
type: verification
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - DEM-01
files_modified:
  - .planning/phases/07-capability-absorption-pipeline/07-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - examples/
  - cavalier_contours_ffi/
  - cavalier_contours_ui/src/scenes/
must_haves:
  truths:
    - "D-08: Every absorbed capability needs focused tests first or with implementation."
    - "D-09: Public Rust API changes require docs and example coverage; FFI changes require ABI impact notes and header regeneration only when ABI changes."
    - "D-10: Demo UI changes are allowed only when selected capability needs visual validation."
---

# Plan 07-04: Update External Surfaces and Verify Phase

<objective>
Finish Phase 7 by updating only the external surfaces required by the selected
capability and recording full verification evidence.
</objective>

<context>
@.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-CANDIDATES.md
@.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md
@.planning/phases/07-capability-absorption-pipeline/07-03-SUMMARY.md
@.planning/codebase/INTEGRATIONS.md
@AGENTS.md
</context>

<tasks>

<task>
<name>Update required external surfaces</name>
<read_first>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
- `.planning/codebase/INTEGRATIONS.md`
- `AGENTS.md`
- Any example, FFI, or UI file named by `07-CAPABILITY-DESIGN.md`
</read_first>
<files>
- `examples/` only if `Example/docs impact` is not `none`
- `cavalier_contours_ffi/src/lib.rs` and `cavalier_contours_ffi.h` only if `FFI impact` is not `none`
- `cavalier_contours_ui/src/scenes/` only if `UI impact` is `required`
</files>
<action>
Follow the impact fields in `07-CAPABILITY-DESIGN.md`. Add or update examples
and docs for public Rust API changes. Add FFI code, ABI tests, and regenerate
`cavalier_contours_ffi.h` only if the design records a real FFI ABI change.
Touch UI scenes only if the design marks UI impact as required.
</action>
<acceptance_criteria>
- If `Example/docs impact` is not `none`, an example or docs file contains the
  selected candidate ID or public API name.
- If `FFI impact` is `none`, `git diff --name-only` does not include
  `cavalier_contours_ffi.h`.
- If `UI impact` is not `required`, `git diff --name-only` does not include
  `cavalier_contours_ui/src/scenes/`.
</acceptance_criteria>
<verify>
`git diff --name-only`
</verify>
</task>

<task>
<name>Run final verification and complete phase records</name>
<read_first>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-CANDIDATES.md`
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
- `.planning/phases/07-capability-absorption-pipeline/07-01-SUMMARY.md`
- `.planning/phases/07-capability-absorption-pipeline/07-02-SUMMARY.md`
- `.planning/phases/07-capability-absorption-pipeline/07-03-SUMMARY.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
</read_first>
<files>
- `.planning/phases/07-capability-absorption-pipeline/07-VERIFICATION.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
</files>
<action>
Run the final gate set and record command results in `07-VERIFICATION.md`.
After all gates pass, mark `CAP-01`, `CAP-02`, `CAP-03`, and `DEM-01` complete
and complete Phase 7 through GSD state tooling.
</action>
<acceptance_criteria>
- `07-VERIFICATION.md` contains `CAP-01`, `CAP-02`, `CAP-03`, and `DEM-01`.
- `cargo test --workspace`, `cargo fmt --all --check`, and
  `cargo clippy --all-targets -- -D warnings` pass.
- `gsd-sdk query state.validate` returns `valid: true`.
- `gsd-sdk query validate.health --project-dir E:\Coding\cavalier_contours`
  returns `status: healthy`.
</acceptance_criteria>
<verify>
`gsd-sdk query validate.health --project-dir E:\Coding\cavalier_contours`
</verify>
</task>

</tasks>

<verification>

- Targeted test command from `07-CAPABILITY-DESIGN.md`
- `cargo test --workspace`
- `cargo fmt --all --check`
- `cargo clippy --all-targets -- -D warnings`
- `git diff --check`
- `git status --short -- target cavalier_contours/target`
- `gsd-sdk query state.validate`
- `gsd-sdk query validate.health --project-dir E:\Coding\cavalier_contours`

</verification>

<output>
After completion, create `.planning/phases/07-capability-absorption-pipeline/07-04-SUMMARY.md`.
</output>

