---
phase: 07
plan: 07-02
type: design
wave: 2
depends_on:
  - 07-01
autonomous: true
requirements:
  - CAP-02
  - CAP-03
  - DEM-01
files_modified:
  - .planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md
must_haves:
  truths:
    - "D-05: Clipper2-derived capabilities must preserve the Rust arc-aware model or be documented as polygon-only."
    - "D-07: Prefer existing public patterns and modules over new abstractions."
    - "D-09: Public Rust API changes require docs and example coverage; FFI changes require ABI impact notes."
    - "D-10: Demo UI changes are allowed only when visual validation is needed."
---

# Plan 07-02: Design Capability Boundary

<objective>
Turn the selected candidate from 07-01 into a concrete behavior, API, test, FFI,
and UI impact contract before implementation.
</objective>

<context>
@.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-CANDIDATES.md
@.planning/phases/07-capability-absorption-pipeline/07-CONTEXT.md
@.planning/phases/07-capability-absorption-pipeline/07-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>

<task>
<name>Write selected capability design contract</name>
<read_first>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-CANDIDATES.md`
- `.planning/phases/07-capability-absorption-pipeline/07-CONTEXT.md`
- `.planning/phases/07-capability-absorption-pipeline/07-PATTERNS.md`
- `.planning/codebase/ARCHITECTURE.md`
- `.planning/codebase/INTEGRATIONS.md`
</read_first>
<files>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
</files>
<action>
Create `07-CAPABILITY-DESIGN.md`. Record `Selected candidate`, source
provenance, behavior contract, semantic fit (`arc-aware` or `polygon-only` with
rationale), implementation files, test files, targeted test command, public Rust
API impact, FFI impact, generated header impact, example/docs impact, and UI
impact.
</action>
<acceptance_criteria>
- `07-CAPABILITY-DESIGN.md` contains `Selected candidate`, `Semantic fit`,
  `Implementation files`, `Targeted test command`, `FFI impact`, `Generated
  header impact`, and `UI impact`.
- If `FFI impact` is not `none`, the design names `cavalier_contours_ffi/src/lib.rs`
  and `cavalier_contours_ffi.h`.
- If `UI impact` is `required`, the design names the affected
  `cavalier_contours_ui/src/scenes/` file.
</acceptance_criteria>
<verify>
`Select-String -Path .planning\phases\07-capability-absorption-pipeline\07-CAPABILITY-DESIGN.md -Pattern "Selected candidate","Semantic fit","Implementation files","Targeted test command","FFI impact","Generated header impact","UI impact"`
</verify>
</task>

<task>
<name>Define execution guardrails</name>
<read_first>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
- `AGENTS.md`
</read_first>
<files>
- `.planning/phases/07-capability-absorption-pipeline/07-CAPABILITY-DESIGN.md`
</files>
<action>
Add an `Execution Guardrails` section stating that implementation must avoid
unsafe code in the core crate, avoid production Clipper2 dependency, avoid broad
corpus parsers, avoid FFI header regeneration unless the ABI changes, and keep
UI untouched unless the design marks UI impact as required.
</action>
<acceptance_criteria>
- The guardrails section contains `unsafe code`, `production Clipper2`,
  `broad corpus`, `FFI header`, and `UI impact`.
</acceptance_criteria>
<verify>
`Select-String -Path .planning\phases\07-capability-absorption-pipeline\07-CAPABILITY-DESIGN.md -Pattern "unsafe code","production Clipper2","broad corpus","FFI header","UI impact"`
</verify>
</task>

</tasks>

<verification>

- `Select-String -Path .planning\phases\07-capability-absorption-pipeline\07-CAPABILITY-DESIGN.md -Pattern "CAP-02","CAP-03","DEM-01"`
- `git diff --check`

</verification>

<output>
After completion, create `.planning/phases/07-capability-absorption-pipeline/07-02-SUMMARY.md`.
</output>

