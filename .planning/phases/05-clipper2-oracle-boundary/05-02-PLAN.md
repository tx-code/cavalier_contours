---
phase: 05
plan: 05-02
type: implementation
wave: 2
depends_on:
  - 05-01
autonomous: true
requirements:
  - FIX-04
  - ORC-01
  - ORC-02
files_modified:
  - cavalier_contours/tests/test_clipper2_oracle_fixtures.rs
must_haves:
  truths:
    - "D-02: Use Clipper2 expected test data and documented public behavior as oracle evidence; do not copy implementation code."
    - "D-04: Eligible cases are closed polygon paths with straight segments."
    - "D-05: Boolean oracle cases must map to current Rust boolean semantics: two closed area polylines, supported operation, no open-path result requirement, and property-based comparison."
    - "D-06: Offset oracle cases record Clipper2 join/end type, delta, and approximation policy."
    - "D-09: Reuse existing fixture harness and metadata model where possible."
    - "D-14: No production source, FFI header, UI, or benchmark baseline changes are expected."
  artifacts:
    - "cavalier_contours/tests/test_clipper2_oracle_fixtures.rs"
  key_links:
    - "Fixture IDs match 05-CLIPPER2-INVENTORY.md selected case rows."
---

# Plan 05-02: Add Clipper2 Oracle Fixtures

<objective>
Represent the curated Clipper2 oracle cases as Rust test-only fixtures with
provenance, polygon comparability classification, and explicit offset
approximation notes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-clipper2-oracle-boundary/05-CLIPPER2-INVENTORY.md
@.planning/phases/05-clipper2-oracle-boundary/05-CONTEXT.md
@.planning/phases/05-clipper2-oracle-boundary/05-PATTERNS.md
@cavalier_contours/tests/test_utils/fixture_schema.rs
@cavalier_contours/tests/test_utils/fixture_harness.rs
@cavalier_contours/tests/test_historical_cavalier_contours.rs
</context>

<threat_model>

- **T-05-03: Test-only oracle data changes production behavior.**
  Mitigation: only add an integration test under `cavalier_contours/tests/`.
- **T-05-04: Offset semantics are compared without recording Clipper2
  join/end/delta mapping.**
  Mitigation: fixture comments and evidence rows must name JoinType, EndType,
  Clipper2 delta, Rust offset sign, and approximation status.

</threat_model>

<tasks>

<task>
<name>Add Clipper2 fixture test file</name>
<files>
- `cavalier_contours/tests/test_clipper2_oracle_fixtures.rs`
</files>
<read_first>
- `cavalier_contours/tests/test_historical_cavalier_contours.rs`
- `cavalier_contours/tests/test_utils/fixture_schema.rs`
- `cavalier_contours/tests/test_utils/fixture_harness.rs`
</read_first>
<action>
Create the test file with `mod test_utils;`, Clipper2 provenance constants,
a `clipper2_provenance` helper, fixture property option helpers, and fixture
builders matching the selected IDs from `05-CLIPPER2-INVENTORY.md`.
</action>
<verify>
`cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
</verify>
<acceptance_criteria>
- The file contains the Clipper2 commit hash and Boost license string.
- All fixture provenance uses `UsageLabel::OracleComparable`.
- No production source, FFI, UI, benchmark, or generated header file changes.
</acceptance_criteria>
<done>
The Clipper2 oracle fixture surface exists in test-only code.
</done>
</task>

<task>
<name>Implement executable boolean and offset fixtures</name>
<files>
- `cavalier_contours/tests/test_clipper2_oracle_fixtures.rs`
</files>
<read_first>
- `.planning/phases/05-clipper2-oracle-boundary/05-CLIPPER2-INVENTORY.md`
- `E:/Coding/Clipper2/CPP/Tests/TestPolytreeIntersection.cpp`
- `E:/Coding/Clipper2/CPP/Tests/TestOffsets.cpp`
</read_first>
<action>
Implement `clipper2-polytree-intersection-square-overlap` as a
`FixtureOperation::Boolean` with `BooleanOp::And`, expected one polygon with
area `16`, path length `16`, and extents `(1,1)-(5,5)`. Implement
`clipper2-offset-007-collapsed-square` as a `FixtureOperation::Offset` using
the square from `TestOffsets7`, mapping Clipper2 `InflatePaths(..., -50,
JoinType::Miter, EndType::Polygon)` to the current Rust interior offset result
with expected empty output. Set comparison to `ApproximateParity`.
</action>
<verify>
`cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
</verify>
<acceptance_criteria>
- At least one boolean and one offset fixture execute through `run_fixture`.
- Offset comments record JoinType, EndType, Clipper2 delta, and Rust sign
  mapping.
- Arc-to-polygon approximation is recorded as not used for the selected
  straight-segment cases.
</acceptance_criteria>
<done>
Phase 5 has executable Clipper2-derived oracle fixtures.
</done>
</task>

<task>
<name>Add metadata-only oracle records</name>
<files>
- `cavalier_contours/tests/test_clipper2_oracle_fixtures.rs`
</files>
<read_first>
- `.planning/phases/05-clipper2-oracle-boundary/05-CLIPPER2-INVENTORY.md`
- `E:/Coding/Clipper2/Tests/Polygons.txt`
- `E:/Coding/Clipper2/Tests/Offsets.txt`
</read_first>
<action>
Add metadata-only records for selected broader Clipper2 cases, including
`clipper2-polygons-017-intersection-evenodd` and
`clipper2-offsets-001-round-polygon`. Use `ComparisonMode::NotComparable` or
`Gap` only as justified by the inventory, and assert that metadata-only records
do not execute.
</action>
<verify>
`cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
</verify>
<acceptance_criteria>
- Metadata-only records expose source path, operation kind, comparison mode,
  and executable flag via `fixture_metadata`.
- Broad Clipper2 text fixtures are visible but do not become red tests.
</acceptance_criteria>
<done>
The oracle corpus includes both executable evidence and non-executable boundary
records.
</done>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
- `cargo fmt --all --check`
- `git diff --check`

</verification>

<success_criteria>

- Clipper2 fixture records are test-only and provenance-rich.
- Boolean and offset oracle examples can run through the existing fixture
  harness.
- Offset approximation and sign-mapping details are explicit.

</success_criteria>

<output>
After completion, create `.planning/phases/05-clipper2-oracle-boundary/05-02-SUMMARY.md`.
</output>
