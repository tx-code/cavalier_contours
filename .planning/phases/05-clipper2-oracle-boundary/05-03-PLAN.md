---
phase: 05
plan: 05-03
type: implementation
wave: 3
depends_on:
  - 05-02
autonomous: true
requirements:
  - ORC-01
  - ORC-03
files_modified:
  - cavalier_contours/tests/test_clipper2_oracle_fixtures.rs
must_haves:
  truths:
    - "D-03: The dev-only comparison path may be Rust-side; live C++ Clipper2 compilation is optional/manual."
    - "D-11: Oracle evidence reports classify selected cases as pass, gap, not comparable, or deferred."
    - "D-13: Generated oracle reports under target/ are local artifacts and must not be committed."
  artifacts:
    - "target/clipper2-oracle/clipper2-oracle-report.md"
  key_links:
    - "Report rows derive from the same selected fixture records as 05-02."
---

# Plan 05-03: Add Dev-Only Oracle Report Path

<objective>
Add a dev-only report path that can classify selected Clipper2 oracle records as
pass, gap, not comparable, or deferred without adding Clipper2 as a normal build
or runtime dependency.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-clipper2-oracle-boundary/05-CLIPPER2-INVENTORY.md
@cavalier_contours/tests/test_clipper2_oracle_fixtures.rs
@cavalier_contours/tests/test_utils/fixture_harness.rs
</context>

<threat_model>

- **T-05-05: Report generation becomes a required production or CI runtime
  dependency.**
  Mitigation: trigger report writing only through an environment variable in
  the integration test.
- **T-05-06: Oracle evidence is mistaken for production acceptance.**
  Mitigation: report language states that results are Phase 6 gap-ranking
  evidence only.

</threat_model>

<tasks>

<task>
<name>Add oracle classification model</name>
<files>
- `cavalier_contours/tests/test_clipper2_oracle_fixtures.rs`
</files>
<read_first>
- `cavalier_contours/tests/test_clipper2_oracle_fixtures.rs`
- `cavalier_contours/tests/test_utils/fixture_harness.rs`
</read_first>
<action>
Add small test-local types or helper functions that classify executable
fixtures as `pass` after `run_fixture`, metadata-only not-comparable records as
`not comparable`, and explicitly deferred records as `deferred`. Keep this code
inside the integration test.
</action>
<verify>
`cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
</verify>
<acceptance_criteria>
- Classifications are derived from fixture metadata and execution behavior.
- No production API or schema changes are required unless implementation proves
  the local helper is insufficient.
</acceptance_criteria>
<done>
Oracle records have an observable dev-only status model.
</done>
</task>

<task>
<name>Write local report when requested</name>
<files>
- `cavalier_contours/tests/test_clipper2_oracle_fixtures.rs`
</files>
<read_first>
- `.planning/phases/05-clipper2-oracle-boundary/05-VALIDATION.md`
</read_first>
<action>
Add a test that writes `target/clipper2-oracle/clipper2-oracle-report.md` only
when `CAVC_CLIPPER2_ORACLE_REPORT=1`. The report must include fixture id,
operation kind, source path, comparison mode, status, and note that oracle
results are Phase 6 gap-ranking evidence, not production behavior.
</action>
<verify>
`$env:CAVC_CLIPPER2_ORACLE_REPORT="1"; cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
</verify>
<acceptance_criteria>
- Report command exits 0.
- Report file exists under `target/clipper2-oracle/`.
- `git status --short -- target` shows no committed generated report output.
</acceptance_criteria>
<done>
Developers can regenerate local Clipper2 oracle evidence on demand.
</done>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
- `$env:CAVC_CLIPPER2_ORACLE_REPORT="1"; cargo test -p cavalier_contours --test test_clipper2_oracle_fixtures -- --nocapture`
- `git status --short -- target`
- `git diff --check`

</verification>

<success_criteria>

- ORC-01 has a dev-only comparison/report path.
- ORC-03 is visible in generated report text and committed evidence docs.
- Generated target output remains untracked.

</success_criteria>

<output>
After completion, create `.planning/phases/05-clipper2-oracle-boundary/05-03-SUMMARY.md`.
</output>

