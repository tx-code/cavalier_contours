---
phase: 02
plan: 02-03
type: implementation
wave: 3
depends_on:
  - 02-01
  - 02-02
autonomous: true
requirements:
  - FIX-01
  - FIX-02
files_modified:
  - cavalier_contours/tests/test_fixture_harness.rs
must_haves:
  truths:
    - "D-03: Add seed proof tests in cavalier_contours/tests/test_fixture_harness.rs."
    - "D-06: Seed fixtures include full provenance."
    - "D-07: Seeds use explicit geometry model values."
    - "D-08: Seeds use operation-specific input structs."
    - "D-09: Executable seeds include required expected properties."
    - "D-10: Seed suite demonstrates full taxonomy representation."
    - "D-11: Metadata-only seed does not execute assertions by default."
    - "D-12: Seeds use the centralized tolerance policy/helper."
    - "D-14: Seeds prove layered default property assertions and any required opt-ins."
    - "D-15: Seeds execute through generic run_fixture(&FixtureCase)."
    - "D-16: Seeds cover offset, boolean, and contains/properties runner paths only."
    - "D-17: Seed failures would include structured fixture output."
    - "D-18: Metadata collector covers the metadata-only seed."
    - "D-19: Seeds use current Rust behavior only."
    - "D-20: Add one seed each for offset, boolean, and contains/properties."
    - "D-21: Add one metadata-only not-comparable or gap seed."
    - "D-22: Avoid external fixture imports, oracle output, and benchmark profiles."
---

# Plan 02-03: Add Current-Rust Seed Fixtures

<objective>
Add a small proof suite that demonstrates the typed schema and fixture harness
work for current Rust behavior. The seeds should prove the harness shape without
starting old C++ mining, Clipper2 oracle work, or benchmark baseline work.
</objective>

<tasks>

<task>
<name>Add fixture harness integration test</name>
<files>
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<action>
Create a new integration test that imports `mod test_utils;` and builds typed
`FixtureCase` values.
</action>
<verify>
`cargo test -p cavalier_contours --test test_fixture_harness` runs the seed
suite.
</verify>
<done>
Test file lives beside existing integration tests.
</done>
</task>

<task>
<name>Add current Rust offset seed</name>
<files>
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<action>
Add one simple offset fixture with current-Rust provenance, expected property
set, geometry model, operation options, comparison mode, and tolerance policy.
</action>
<verify>
Seed passes through `run_fixture(&FixtureCase)`.
</verify>
<done>
Demonstrates executable exact/approx property comparison for offset.
</done>
</task>

<task>
<name>Add current Rust boolean seed</name>
<files>
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<action>
Add one simple boolean fixture using existing Rust behavior and property
comparison, including any required abs-area or result-set options.
</action>
<verify>
Seed passes through `run_fixture(&FixtureCase)`.
</verify>
<done>
Demonstrates boolean operation dispatch and property comparison without literal
vertex-order assertions.
</done>
</task>

<task>
<name>Add contains/properties seed</name>
<files>
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<action>
Add one fixture that proves the contains/properties runner path and expected
property handling.
</action>
<verify>
Seed passes through `run_fixture(&FixtureCase)`.
</verify>
<done>
Demonstrates the third Phase 2 runner path.
</done>
</task>

<task>
<name>Add metadata-only taxonomy seed</name>
<files>
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<action>
Add one `not comparable` or `gap` fixture with full provenance and metadata;
assert that metadata collection reports it as non-executable.
</action>
<verify>
The fixture does not execute behavior assertions and does not create
ignored/failing tests.
</verify>
<done>
Full taxonomy can be recorded without red tests.
</done>
</task>

</tasks>

<must_haves>

- D-03: Add seed proof tests in `cavalier_contours/tests/test_fixture_harness.rs`.
- D-06: Seed fixtures include full provenance.
- D-07: Seeds use explicit geometry model values.
- D-08: Seeds use operation-specific input structs.
- D-09: Executable seeds include required expected properties.
- D-10: Seed suite demonstrates full taxonomy representation.
- D-11: Metadata-only seed does not execute assertions by default.
- D-12: Seeds use the centralized tolerance policy/helper.
- D-14: Seeds prove layered default property assertions and any required opt-ins.
- D-15: Seeds execute through generic `run_fixture(&FixtureCase)`.
- D-16: Seeds cover offset, boolean, and contains/properties runner paths only.
- D-17: Seed failures would include structured fixture output.
- D-18: Metadata collector covers the metadata-only seed.
- D-19: Seeds use current Rust behavior only.
- D-20: Add one seed each for offset, boolean, and contains/properties.
- D-21: Add one metadata-only not-comparable or gap seed.
- D-22: Avoid external fixture imports, oracle output, and benchmark profiles.
- Seed provenance must identify current Rust source as `fork-owned/changeable`.
- Do not add old C++ fixtures, Clipper2 fixtures, oracle output, benchmark
  profiles, or file-based fixture parser work.

</must_haves>

<verification>

- `cargo test -p cavalier_contours --test test_fixture_harness`
- `cargo test -p cavalier_contours`
- `cargo fmt --all --check`
- `git diff --check`
</verification>

<success_criteria>

- Offset, boolean, and contains/properties runner paths each have one current
  Rust seed.
- One metadata-only taxonomy seed proves non-executable fixture recording.
- The seed suite proves both `FIX-01` and `FIX-02` without expanding phase
  scope.
</success_criteria>
