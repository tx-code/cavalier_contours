---
phase: 99
plan: 99-29
type: implementation
wave: 29
depends_on:
  - 99-28
autonomous: true
requirements:
  - PAR-289
files_modified:
  - cavalier_contours/src/polyline/internal/pline_intersects.rs
  - .planning/phases/99-cpp-pline-seg-overlap-order-and-endpoint-stickiness-parity/99-CPP-LOGIC-ALIGNMENT-MAP.md
must_haves:
  truths:
    - "Collection-level `find_intersects` explicitly guards line-line `False` branch with overlapping AABB candidates."
    - "Collection-level `find_intersects` explicitly guards line-line `None` branch with overlapping AABB candidates."
---

# Plan 99-29: Add Collection-Level Line-Line Non-Overlap Guards

<objective>
Add source-aligned collection-level guards in `find_intersects_tests` for
line-line `False` and `None` outcomes (with overlapping AABB candidates), then
refresh the Phase 99 alignment map and verification evidence.
</objective>
