---
phase: 99
plan: 99-26
type: implementation
wave: 26
depends_on:
  - 99-25
autonomous: true
requirements:
  - PAR-286
files_modified:
  - cavalier_contours/tests/test_pline_seg_intersect.rs
  - .planning/phases/99-cpp-pline-seg-overlap-order-and-endpoint-stickiness-parity/99-CPP-LOGIC-ALIGNMENT-MAP.md
must_haves:
  truths:
    - "Line-arc `processLineArcIntr` (`numIntersects == 2`) has explicit one-in-sweep filtering probes without endpoint-stickiness confounds."
    - "Line-arc `processLineArcIntr` (`numIntersects == 2`) has explicit both-outside-sweep filtering probes for both dispatch paths."
---

# Plan 99-26: Cover Line-Arc Two-Intersect Sweep Filtering

<objective>
Add source-aligned segment-level probes for old C++ `intrPlineSegs`
`processLineArcIntr` `numIntersects == 2` sweep-classification branches
(one-in-sweep vs both-outside-sweep) in non-endpoint-sticky geometry for both
line-arc and arc-line dispatch, then refresh the Phase 99 alignment map and
verification evidence.
</objective>
