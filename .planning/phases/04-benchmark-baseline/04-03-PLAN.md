---
phase: 04
plan: 04-03
type: implementation
wave: 3
depends_on:
  - 04-01
  - 04-02
autonomous: true
requirements:
  - BEN-01
  - BEN-02
  - BEN-03
files_modified:
  - .planning/phases/04-benchmark-baseline/04-BENCHMARKS.md
  - .planning/phases/04-benchmark-baseline/04-VERIFICATION.md
must_haves:
  truths:
    - "D-03: Generated benchmark output directories are not committed."
    - "D-08: Clipper2 runtime/oracle costs remain out of Phase 4."
    - "D-09: Baseline documentation states environment, command, harness mode, profile settings, and cost inclusion/exclusion."
    - "D-10: Verification includes workspace gates plus a practical benchmark smoke command."
    - "D-11: No production geometry behavior changes are part of Phase 4."
  artifacts:
    - ".planning/phases/04-benchmark-baseline/04-BENCHMARKS.md"
    - ".planning/phases/04-benchmark-baseline/04-VERIFICATION.md"
  key_links:
    - "04-BENCHMARKS.md records the final command/provenance contract."
    - "04-VERIFICATION.md records the smoke and workspace gate outcomes."
---

# Plan 04-03: Document Benchmark Provenance and Cost Accounting

<objective>
Finalize Phase 4 by recording repeatable benchmark commands, environment
fields, cost-accounting rules, and verification evidence for the benchmark
baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-benchmark-baseline/04-CONTEXT.md
@.planning/phases/04-benchmark-baseline/04-RESEARCH.md
@.planning/phases/04-benchmark-baseline/04-VALIDATION.md
@.planning/phases/04-benchmark-baseline/04-BENCHMARKS.md
@.planning/phases/04-benchmark-baseline/04-BENCHMARK-MAP.md
@.planning/codebase/TESTING.md
</context>

<threat_model>

- **T-04-07: Benchmark output exists but cannot be repeated.**
  Mitigation: docs record exact commands, harness mode, rustc version command,
  date, platform fields, and generated-output policy.
- **T-04-08: Baseline numbers are treated as pass/fail thresholds.**
  Mitigation: docs state Phase 4 records coverage and measurement provenance,
  not regression budgets.
- **T-04-09: Scope drift enters production code or FFI.**
  Mitigation: final verification checks changed paths and confirms no FFI
  header regeneration.

</threat_model>

<tasks>

<task>
<name>Record cost-accounting rules</name>
<files>
- `.planning/phases/04-benchmark-baseline/04-BENCHMARKS.md`
</files>
<read_first>
- `.planning/phases/04-benchmark-baseline/04-CONTEXT.md`
- `.planning/phases/04-benchmark-baseline/04-BENCHMARK-MAP.md`
</read_first>
<action>
Update `04-BENCHMARKS.md` with explicit included/excluded costs for setup,
native arcs, no-arcs conversion, oracle execution, Criterion harness overhead,
and generated output. State that conversion is excluded for no-arcs variants
unless a benchmark ID explicitly says otherwise.
</action>
<verify>
`Select-String -Path .planning\phases\04-benchmark-baseline\04-BENCHMARKS.md -Pattern "setup cost","conversion cost","oracle cost","native arc","no-arcs","target/criterion"`
</verify>
<acceptance_criteria>
- Cost rules are explicit enough to decide whether a future benchmark ID is
  comparable to this baseline.
- Clipper2 oracle/runtime is clearly excluded.
- Generated Criterion output remains uncommitted.
</acceptance_criteria>
<done>
BEN-03 cost accounting is documented.
</done>
</task>

<task>
<name>Capture repeatable environment and command record</name>
<files>
- `.planning/phases/04-benchmark-baseline/04-BENCHMARKS.md`
</files>
<read_first>
- `.planning/codebase/TESTING.md`
- `Cargo.toml`
- `cavalier_contours/Cargo.toml`
</read_first>
<action>
Record the smoke command, full local baseline command, and environment fields
to capture: date, OS, CPU if available, `rustc -Vv`, `cargo -V`, git commit,
Cargo profile settings, benchmark target, and whether `-- --test` smoke mode or
full Criterion measurement mode was used. If a full benchmark run is practical,
run it locally and summarize only the source-visible result/provenance, not
generated output.
</action>
<verify>
`Select-String -Path .planning\phases\04-benchmark-baseline\04-BENCHMARKS.md -Pattern "rustc -Vv","cargo -V","git rev-parse HEAD","cargo bench -p cavalier_contours --bench geometry_baseline -- --test","cargo bench -p cavalier_contours --bench geometry_baseline"`
</verify>
<acceptance_criteria>
- Anyone can rerun the smoke and full local commands from the doc.
- The doc distinguishes smoke mode from full measurement mode.
- The doc does not include generated Criterion raw output.
</acceptance_criteria>
<done>
The benchmark baseline has a repeatable provenance record.
</done>
</task>

<task>
<name>Run final verification and record evidence</name>
<files>
- `.planning/phases/04-benchmark-baseline/04-VERIFICATION.md`
- `.planning/phases/04-benchmark-baseline/04-BENCHMARKS.md`
</files>
<read_first>
- `.planning/phases/04-benchmark-baseline/04-VALIDATION.md`
- `.planning/phases/04-benchmark-baseline/04-BENCHMARK-MAP.md`
- `.planning/phases/04-benchmark-baseline/04-BENCHMARKS.md`
</read_first>
<action>
Run the final Phase 4 gates and record outcomes in `04-VERIFICATION.md`.
Confirm changed paths are scoped to benchmark source/config and planning docs.
Confirm no `cavalier_contours_ffi.h` regeneration and no Clipper2 runtime calls.
</action>
<verify>
`cargo bench -p cavalier_contours --bench geometry_baseline -- --test`
`cargo test --workspace`
`cargo fmt --all --check`
`cargo clippy --all-targets -- -D warnings`
`git diff --check`
</verify>
<acceptance_criteria>
- Final verification records pass/fail evidence for the benchmark smoke and
  normal workspace gates.
- Scope check confirms no production geometry behavior changes unless required
  by the benchmark target compile.
- Phase 4 requirements BEN-01, BEN-02, and BEN-03 are all marked covered in the
  verification narrative.
</acceptance_criteria>
<done>
Phase 4 is ready for GSD verification and completion.
</done>
</task>

</tasks>

<verification>

- `cargo bench -p cavalier_contours --bench geometry_baseline -- --test`
- `cargo test --workspace`
- `cargo fmt --all --check`
- `cargo clippy --all-targets -- -D warnings`
- `git diff --check`
- `git status --short -- target`

</verification>

<success_criteria>

- Benchmark commands, environment fields, and cost-accounting rules are
  documented.
- Smoke benchmark and workspace gates are recorded.
- No generated benchmark output, FFI header regeneration, UI redesign, or
  Clipper2 oracle/runtime work is included in Phase 4.

</success_criteria>

<output>
After completion, create `.planning/phases/04-benchmark-baseline/04-03-SUMMARY.md`.
</output>

