---
phase: 04
plan: 04-01
type: implementation
wave: 1
depends_on: []
autonomous: true
requirements:
  - BEN-01
files_modified:
  - cavalier_contours/Cargo.toml
  - cavalier_contours/benches/geometry_baseline.rs
  - .planning/phases/04-benchmark-baseline/04-BENCHMARKS.md
must_haves:
  truths:
    - "D-01: Use a stable Rust benchmark harness, preferably Criterion."
    - "D-02: Put benchmark definitions under the core crate benchmark surface."
    - "D-03: Do not commit generated benchmark output directories."
    - "D-04: Baseline coverage includes offsets, booleans, intersections, and spatial-index-heavy inputs."
    - "D-10: Verification includes workspace gates plus a practical benchmark compile/smoke command."
    - "D-11: Add dev-only benchmark dependencies only; no production geometry behavior changes."
  artifacts:
    - "cavalier_contours/benches/geometry_baseline.rs"
    - ".planning/phases/04-benchmark-baseline/04-BENCHMARKS.md"
  key_links:
    - "The smoke command in 04-BENCHMARKS.md matches the benchmark target declared in Cargo.toml."
    - "The benchmark target calls current public Rust APIs only."
---

# Plan 04-01: Add Current Rust Measurement Baseline

<objective>
Create the initial stable Rust benchmark surface for current `cavalier_contours`
behavior, covering the operation groups required by BEN-01 without changing
production geometry code.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-benchmark-baseline/04-CONTEXT.md
@.planning/phases/04-benchmark-baseline/04-RESEARCH.md
@.planning/phases/04-benchmark-baseline/04-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
</context>

<threat_model>

- **T-04-01: Benchmark dependency leaks into production runtime.**
  Mitigation: add Criterion only as a dev-dependency and declare the bench
  target with `harness = false`.
- **T-04-02: Benchmarks compile but miss required operation groups.**
  Mitigation: create explicit benchmark groups for offset, boolean,
  intersections, and spatial-index-heavy workloads.
- **T-04-03: Generated measurement output is committed.**
  Mitigation: document generated-output policy and check staged files before
  commit.

</threat_model>

<tasks>

<task>
<name>Add Criterion benchmark target</name>
<files>
- `cavalier_contours/Cargo.toml`
- `cavalier_contours/benches/geometry_baseline.rs`
</files>
<read_first>
- `cavalier_contours/Cargo.toml`
- `Cargo.toml`
- `.planning/phases/04-benchmark-baseline/04-RESEARCH.md`
</read_first>
<action>
Add Criterion as a dev-only dependency for the core crate and declare
`[[bench]] name = "geometry_baseline"` with `harness = false`. Create the
benchmark file with a minimal Criterion entry point.
</action>
<verify>
`cargo bench -p cavalier_contours --bench geometry_baseline -- --test`
</verify>
<acceptance_criteria>
- `cavalier_contours/Cargo.toml` leaves production `[dependencies]` unchanged.
- The benchmark target compiles and runs in Criterion smoke mode.
- No `target/criterion` output is staged or committed.
</acceptance_criteria>
<done>
The repository has a stable benchmark entry point for the core crate.
</done>
</task>

<task>
<name>Cover current Rust operation groups</name>
<files>
- `cavalier_contours/benches/geometry_baseline.rs`
</files>
<read_first>
- `cavalier_contours/src/polyline/traits.rs`
- `cavalier_contours/tests/test_pline_parallel_offset.rs`
- `cavalier_contours/tests/test_pline_boolean.rs`
- `cavalier_contours/tests/test_line_line_intersect.rs`
- `cavalier_contours/tests/test_line_circle_intersect.rs`
- `cavalier_contours/tests/test_circle_circle_intersect.rs`
</read_first>
<action>
Add small current-Rust benchmark cases for:
offsets via `parallel_offset`, booleans via `boolean`, intersections via
`find_intersects` or public segment/intersection helpers, and spatial-index-heavy
work via `create_approx_aabb_index` plus representative query/reuse behavior.
Use `criterion::black_box`, deterministic input builders, and explicit group
IDs.
</action>
<verify>
`cargo bench -p cavalier_contours --bench geometry_baseline -- --test`
</verify>
<acceptance_criteria>
- Benchmark IDs include recognizable `offset`, `boolean`, `intersections`, and
  `spatial_index` groups.
- Inputs are deterministic and built outside the timed loop when setup is not
  the measured behavior.
- Benchmarks use public crate APIs; no test-only harness code is imported.
</acceptance_criteria>
<done>
BEN-01 has executable benchmark coverage for current Rust behavior.
</done>
</task>

<task>
<name>Document baseline commands and generated-output policy</name>
<files>
- `.planning/phases/04-benchmark-baseline/04-BENCHMARKS.md`
</files>
<read_first>
- `.planning/phases/04-benchmark-baseline/04-CONTEXT.md`
- `.planning/phases/04-benchmark-baseline/04-RESEARCH.md`
- `.planning/codebase/TESTING.md`
</read_first>
<action>
Create `04-BENCHMARKS.md` with sections for benchmark target, smoke command,
full local baseline command, generated-output policy, environment fields to
record, and Phase 4 exclusions.
</action>
<verify>
`Select-String -Path .planning\phases\04-benchmark-baseline\04-BENCHMARKS.md -Pattern "cargo bench -p cavalier_contours --bench geometry_baseline -- --test","cargo bench -p cavalier_contours --bench geometry_baseline","target/criterion","Clipper2"`
</verify>
<acceptance_criteria>
- Docs name both the smoke command and full measurement command.
- Docs state that generated Criterion output is not committed.
- Docs state that Clipper2/oracle costs are excluded in Phase 4.
</acceptance_criteria>
<done>
The initial benchmark surface is documented enough for local repeatability.
</done>
</task>

</tasks>

<verification>

- `cargo bench -p cavalier_contours --bench geometry_baseline -- --test`
- `git diff --check`
- `git status --short -- target`

</verification>

<success_criteria>

- A Criterion benchmark target exists under `cavalier_contours/benches/`.
- BEN-01 operation groups are represented by executable current-Rust cases.
- Benchmark commands and generated-output policy are documented.

</success_criteria>

<output>
After completion, create `.planning/phases/04-benchmark-baseline/04-01-SUMMARY.md`.
</output>

