---
phase: 08
plan: 08-03
type: verification
wave: 3
depends_on:
  - 08-01
  - 08-02
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
files_modified:
  - MIGRATION.md
  - .planning/phases/08-api-ffi-and-migration-readiness/08-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
must_haves:
  truths:
    - "D-05: Migration notes must map old C++ concepts to Rust/FFI usage."
    - "D-08: Full workspace and GSD gates must pass before phase completion."
---

# Plan 08-03: Publish Migration Notes and Close Phase

<objective>
Produce migration guidance and finalize Phase 8 with full verification and
requirements closure.
</objective>

<context>
@.planning/phases/08-api-ffi-and-migration-readiness/08-CONTEXT.md
@.planning/phases/08-api-ffi-and-migration-readiness/08-COMPATIBILITY-AUDIT.md
@README.md
@cavalier_contours_ffi/README.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task>
<name>Write migration notes for old C++ users</name>
<files>
- `MIGRATION.md`
</files>
<action>
Add practical migration notes mapping old C++ CavalierContours usage to Rust
crate and C FFI usage, including behavior constraints and adoption steps.
</action>
<acceptance_criteria>
- Migration notes include `CavalierContours`, `Rust`, and `FFI` guidance.
- Migration notes mention current geometry/algorithm limitations relevant to migration.
</acceptance_criteria>
<verify>
`Select-String -Path MIGRATION.md -Pattern "CavalierContours","Rust","FFI","limitations"`
</verify>
</task>

<task>
<name>Run final gates and close requirements</name>
<files>
- `.planning/phases/08-api-ffi-and-migration-readiness/08-VERIFICATION.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
</files>
<action>
Record final command evidence, complete `API-01`, `API-02`, and `API-03`, and
complete Phase 8 through GSD state tooling.
</action>
<acceptance_criteria>
- `08-VERIFICATION.md` contains API requirement closure and command results.
- `cargo test --workspace`, `cargo fmt --all --check`, and
  `cargo clippy --all-targets -- -D warnings` pass.
- `gsd-sdk query state.validate` returns `valid: true`.
- `gsd-sdk query validate.health --project-dir E:\Coding\cavalier_contours`
  returns `status: healthy`.
</acceptance_criteria>
<verify>
`gsd-sdk query validate.health --project-dir E:\Coding\cavalier_contours`
</verify>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_pline_boolean rect_clip -- --nocapture`
- `cargo test --workspace`
- `cargo fmt --all --check`
- `cargo clippy --all-targets -- -D warnings`
- `git diff --check`
- `git status --short -- target cavalier_contours/target`
- `gsd-sdk query state.validate`
- `gsd-sdk query validate.health --project-dir E:\Coding\cavalier_contours`

</verification>

<output>
After completion, create `.planning/phases/08-api-ffi-and-migration-readiness/08-03-SUMMARY.md`.
</output>
