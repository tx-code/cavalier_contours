---
phase: 06
plan: 06-02
type: implementation
wave: 2
depends_on:
  - 06-01
autonomous: true
requirements:
  - ROB-02
files_modified:
  - cavalier_contours/tests/test_shape_parallel_offset.rs
must_haves:
  truths:
    - "D-05: Prefer a small, high-confidence robustness fix over broad boolean or offset rewrites."
    - "D-06: The first fix target is the Shape::from_plines / shape offset input boundary for repeated-position, redundant, or collapsed polylines."
    - "D-07: If the shape input boundary already behaves correctly under focused regressions, keep those tests as coverage and promote the next ranked backlog item rather than inventing a code change."
    - "D-11: Every code change needs a focused targeted test before or with the fix."
  artifacts:
    - "cavalier_contours/tests/test_shape_parallel_offset.rs"
---

# Plan 06-02: Add Focused Shape Robustness Regressions

<objective>
Add targeted regressions for repeated-position, redundant, and collapsed shape
offset inputs before or alongside the production hardening.
</objective>

<context>
@.planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md
@cavalier_contours/tests/test_shape_parallel_offset.rs
@cavalier_contours/tests/test_pline_parallel_offset.rs
@cavalier_contours/src/shape_algorithms/mod.rs
</context>

<tasks>

<task>
<name>Add shape input regression tests</name>
<files>
- `cavalier_contours/tests/test_shape_parallel_offset.rs`
</files>
<action>
Add focused tests that build `Shape::from_plines` with repeated-position,
redundant, and collapsed polylines, then call `parallel_offset`. Include a
mixed valid-plus-degenerate case to prove invalid loops are skipped without
dropping valid loops. Assert no panic and expected property sets.
</action>
<verify>
`cargo test -p cavalier_contours --test test_shape_parallel_offset -- --nocapture`
</verify>
</task>

<task>
<name>Record regression status</name>
<files>
- `.planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md`
</files>
<action>
Update the promoted backlog row with the observed regression status: red before
fix, already green coverage, or promoted-next-candidate if necessary.
</action>
<verify>
`Select-String -Path .planning\phases\06-robustness-gap-closure\06-ROBUSTNESS-BACKLOG.md -Pattern "Regression status"`
</verify>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_shape_parallel_offset -- --nocapture`
- `git diff --check`

</verification>

<output>
After completion, create `.planning/phases/06-robustness-gap-closure/06-02-SUMMARY.md`.
</output>

