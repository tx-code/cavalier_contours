---
phase: 06
plan: 06-03
type: implementation
wave: 3
depends_on:
  - 06-01
  - 06-02
autonomous: true
requirements:
  - ROB-03
files_modified:
  - cavalier_contours/src/shape_algorithms/mod.rs
  - cavalier_contours/tests/test_shape_parallel_offset.rs
must_haves:
  truths:
    - "D-05: Prefer a small, high-confidence robustness fix over broad boolean or offset rewrites."
    - "D-06: The first fix target is the Shape::from_plines / shape offset input boundary for repeated-position, redundant, or collapsed polylines."
    - "D-07: If the shape input boundary already behaves correctly under focused regressions, keep those tests as coverage and promote the next ranked backlog item rather than inventing a code change."
    - "D-11: Every code change needs a focused targeted test before or with the fix."
---

# Plan 06-03: Harden Shape Input Boundary

<objective>
Fix the first ranked current Rust robustness issue with a narrow change to shape
input handling and no public API churn.
</objective>

<context>
@.planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md
@cavalier_contours/src/shape_algorithms/mod.rs
@cavalier_contours/src/polyline/traits.rs
@cavalier_contours/tests/test_shape_parallel_offset.rs
</context>

<tasks>

<task>
<name>Sanitize or skip invalid shape input loops</name>
<files>
- `cavalier_contours/src/shape_algorithms/mod.rs`
</files>
<action>
Update `Shape::from_plines` or a narrow helper it calls so repeat-position,
redundant, or collapsed input polylines are normalized or skipped before
`IndexedPolyline` and spatial-index bounds are built. Preserve valid loop
orientation and user data. Do not change public API signatures.
</action>
<verify>
`cargo test -p cavalier_contours --test test_shape_parallel_offset -- --nocapture`
</verify>
</task>

<task>
<name>Keep fix narrowly scoped</name>
<files>
- `cavalier_contours/src/shape_algorithms/mod.rs`
- `cavalier_contours/tests/test_shape_parallel_offset.rs`
</files>
<action>
If the first regression is already green, do not make a speculative production
change. Instead update the backlog and tests to mark coverage added and promote
the next ranked candidate only if it has a concrete failing regression in this
phase.
</action>
<verify>
`git diff --name-only`
</verify>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_shape_parallel_offset -- --nocapture`
- `cargo fmt --all --check`
- `git diff --check`

</verification>

<output>
After completion, create `.planning/phases/06-robustness-gap-closure/06-03-SUMMARY.md`.
</output>

