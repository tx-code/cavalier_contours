---
phase: 06
plan: 06-01
type: documentation
wave: 1
depends_on: []
autonomous: true
requirements:
  - ROB-01
files_modified:
  - .planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md
must_haves:
  truths:
    - "D-01: Build an explicit ranked robustness backlog before changing algorithm code."
    - "D-02: Rank candidates by user-visible correctness risk, evidence quality, reproducibility, blast radius, and fit with current Rust semantics."
    - "D-03: Treat metadata-only gaps as evidence; do not force a fix when the observed difference is property-equivalent or intentionally divergent."
    - "D-04: Phase 6 must cover offsets, booleans, intersections, tolerances, degenerates, repeat vertices, tangencies, overlaps, and open/closed behavior in the backlog."
    - "D-08: The old C++ circle/rectangle boolean vertex-count mismatch remains a gap record unless Phase 6 proves a real property or topology failure."
    - "D-09: Clipper2 broad text fixtures such as Polygons.txt case 17 are candidates for future robustness evidence, but should not drive broad parser work or a production Clipper2 dependency."
    - "D-10: Performance benchmark data informs prioritization, not optimization work, in this phase."
  artifacts:
    - ".planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md"
---

# Plan 06-01: Build Ranked Robustness Backlog

<objective>
Convert Phase 3-5 evidence into a ranked robustness backlog that explicitly
covers the required risk families and selects the first fix target.
</objective>

<context>
@.planning/phases/06-robustness-gap-closure/06-CONTEXT.md
@.planning/phases/06-robustness-gap-closure/06-RESEARCH.md
@.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md
@.planning/phases/04-benchmark-baseline/04-BENCHMARK-MAP.md
@.planning/phases/05-clipper2-oracle-boundary/05-ORACLE-EVIDENCE.md
@cavalier_contours/src/shape_algorithms/mod.rs
@cavalier_contours/tests/test_shape_parallel_offset.rs
</context>

<tasks>

<task>
<name>Create ranked backlog</name>
<files>
- `.planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md`
</files>
<action>
Create a backlog with rows for offset, boolean, intersection, tolerance,
degenerate, repeat-vertex, tangent, overlap, and open/closed behavior. Score
each candidate by correctness risk, evidence quality, reproducibility, blast
radius, and semantic fit. Promote `shape-offset-repeat-degenerate-input` as the
first fix target unless evidence proves it is already covered.
</action>
<verify>
`Select-String -Path .planning\phases\06-robustness-gap-closure\06-ROBUSTNESS-BACKLOG.md -Pattern "offset","boolean","intersection","tolerance","degenerate","repeat","tangent","overlap","open/closed","shape-offset-repeat-degenerate-input"`
</verify>
</task>

<task>
<name>Record no-fix decisions</name>
<files>
- `.planning/phases/06-robustness-gap-closure/06-ROBUSTNESS-BACKLOG.md`
</files>
<action>
Record why the old C++ circle/rectangle union vertex-count difference is not
the first production fix unless a property/topology failure is proven. Record
why Clipper2 broad text fixtures and benchmarks inform ranking but do not
become parser or performance work in this phase.
</action>
<verify>
`Select-String -Path .planning\phases\06-robustness-gap-closure\06-ROBUSTNESS-BACKLOG.md -Pattern "historical-cpp-combine-circle-rectangle-union","clipper2-polygons-017-intersection-evenodd","benchmark"`
</verify>
</task>

</tasks>

<verification>

- `git diff --check`

</verification>

<output>
After completion, create `.planning/phases/06-robustness-gap-closure/06-01-SUMMARY.md`.
</output>

