---
phase: 03
plan: 03-02
type: implementation
wave: 2
depends_on:
  - 03-01
autonomous: true
requirements:
  - FIX-03
files_modified:
  - cavalier_contours/tests/test_utils/fixture_schema.rs
  - cavalier_contours/tests/test_utils/fixture_harness.rs
  - cavalier_contours/tests/test_fixture_harness.rs
  - cavalier_contours/tests/test_historical_cavalier_contours.rs
must_haves:
  truths:
    - "D-01: Executable translated fixtures cover offset, combine/boolean, and polyline property evidence."
    - "D-05: Fixtures are manually curated typed Rust fixtures; no parser, generator, JSON, RON, or TOML is added."
    - "D-06: Imported cases reuse Phase 2 FixtureCase and run_fixture harness where the operation maps cleanly."
    - "D-07: Any new C API or spatial-index classifications stay test-only and metadata-only."
    - "D-08: Executable imported fixtures keep cargo test green."
    - "D-09: Mismatching old C++ cases become metadata-only gap records instead of red tests."
    - "D-10: No algorithm fixes are required in this plan."
    - "D-11: Every imported case cites repo, commit, license, source path, usage label, comparison mode, and tolerance."
    - "D-12: Expected output is property parity, not brittle vertex sequence parity."
    - "D-13: FixtureTolerance::default() is used unless a source-specific tolerance is justified."
  artifacts:
    - "cavalier_contours/tests/test_historical_cavalier_contours.rs"
  key_links:
    - "03-INVENTORY.md selected fixture IDs are implemented as FixtureCase values."
    - "test_historical_cavalier_contours.rs runs executable old C++ fixtures through run_fixture."
---

# Plan 03-02: Translate Curated Historical Fixtures

<objective>
Translate the curated old C++ evidence from Plan 03-01 into typed Rust fixture
cases. Executable cases must run through the Phase 2 harness; C API,
spatial-index, and failing historical candidates must remain metadata-only.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-historical-c-evidence-mining/03-CONTEXT.md
@.planning/phases/03-historical-c-evidence-mining/03-RESEARCH.md
@.planning/phases/03-historical-c-evidence-mining/03-PATTERNS.md
@.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md
@cavalier_contours/tests/test_utils/fixture_schema.rs
@cavalier_contours/tests/test_utils/fixture_harness.rs
@cavalier_contours/tests/test_fixture_harness.rs
</context>

<threat_model>

- **T-03-04: Red historical fixtures block the phase.**
  Mitigation: candidates that fail current Rust behavior are converted to
  `ComparisonMode::Gap` with `ExpectedFixtureData::MetadataOnly`.
- **T-03-05: Test-only schema extension leaks into production API.**
  Mitigation: only files under `cavalier_contours/tests/` may change in this
  plan.
- **T-03-06: Broad source copy creates license/provenance ambiguity.**
  Mitigation: translate small vertex/property tables with explicit MIT
  provenance; do not copy old implementation algorithms.

</threat_model>

<tasks>

<task>
<name>Add test-only historical schema support</name>
<files>
- `cavalier_contours/tests/test_utils/fixture_schema.rs`
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<read_first>
- `cavalier_contours/tests/test_utils/fixture_schema.rs`
- `cavalier_contours/tests/test_fixture_harness.rs`
- `.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md`
</read_first>
<action>
Extend only the test utility schema for Phase 3. Add usage-label variants for
`MigrationSensitive`, `TranslatedFixtureCandidate`, and `BenchmarkCandidate`.
Add a pure `Properties` fixture operation for old C++ `cavc_get_area`,
`cavc_get_path_length`, and extents evidence. Define `PropertiesFixtureInput`
with a single `Polyline<f64>` input and `ExpectedFixtureData::Properties` with
one `PlineProperties` expected value plus `PropertyExpectationOptions`. Update
the existing schema proof test with one current-Rust property fixture so the new
variant is compiled and exercised before historical cases are added.
</action>
<verify>
`cargo test -p cavalier_contours --test test_fixture_harness`
</verify>
<acceptance_criteria>
- No file outside `cavalier_contours/tests/` changes.
- `UsageLabel` can represent migration-sensitive and translated historical evidence.
- `FixtureOperationKind`, `FixtureOperation`, and `ExpectedFixtureData` have matching `Properties` variants.
- `cargo test -p cavalier_contours --test test_fixture_harness` exits 0.
</acceptance_criteria>
<done>
The test-only schema can represent the selected historical evidence without
new dependencies or production API changes.
</done>
</task>

<task>
<name>Run pure property fixtures through the harness</name>
<files>
- `cavalier_contours/tests/test_utils/fixture_harness.rs`
- `cavalier_contours/tests/test_fixture_harness.rs`
</files>
<read_first>
- `cavalier_contours/tests/test_utils/fixture_harness.rs`
- `cavalier_contours/tests/test_utils/pline_test_properties.rs`
- `cavalier_contours/tests/test_fixture_harness.rs`
</read_first>
<action>
Update `run_fixture(&FixtureCase)` to execute the new `Properties` operation.
The runner must build `PlineProperties::from_pline_with_eps` for the input
polyline and compare it against the expected properties with
`property_sets_match_with_options`. Failure output must include fixture id,
provenance path/commit, operation, comparison mode, tolerance, actual
properties, and expected properties. Keep existing offset, boolean,
contains/properties, and metadata-only behavior unchanged.
</action>
<verify>
`cargo test -p cavalier_contours --test test_fixture_harness -- --nocapture`
</verify>
<acceptance_criteria>
- Existing Phase 2 seed fixtures still execute through the runner.
- Any new property seed executes through the runner and reports structured failure context.
- Metadata-only fixtures still return `executed == false`.
</acceptance_criteria>
<done>
The harness can execute the selected historical pure-property evidence when needed.
</done>
</task>

<task>
<name>Translate selected old C++ fixture cases</name>
<files>
- `cavalier_contours/tests/test_historical_cavalier_contours.rs`
</files>
<read_first>
- `.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md`
- `cavalier_contours/tests/test_fixture_harness.rs`
- `E:/Coding/CavalierContours/tests/tests/TEST_cavc_parallel_offset.cpp`
- `E:/Coding/CavalierContours/tests/tests/TEST_cavc_combine_plines.cpp`
- `E:/Coding/CavalierContours/tests/tests/TEST_cavc_pline_function.cpp`
- `E:/Coding/CavalierContours/c_api_include/cavaliercontours.h`
- `E:/Coding/CavalierContours/tests/tests/TEST_staticspatialindex.cpp`
</read_first>
<action>
Create `test_historical_cavalier_contours.rs` with `mod test_utils;`, old C++
provenance helper values, and typed `FixtureCase` builders for the selected IDs
from `03-INVENTORY.md`. Use source repo `CavalierContours`, source commit
`31a012947aa2e7e9474e2ec90502825afe8b99a4`, and license `MIT`. Implement the
closed rectangle inward offset case with input `(0,0,0)`, `(20,0,0)`,
`(20,10,0)`, `(0,10,0)`, offset `2`, and expected property
`vertex_count=4`, `area=96`, `path_length=44`, extents `(2,2)-(18,8)`.
Implement the collapsed rectangle offset candidate with input `(0,0,0)`,
`(120,0,0)`, `(120,40,0)`, `(0,40,0)`, offset `30`, and expected empty offset
result only if it passes. Implement the circle/rectangle union candidate from
old combine mode `0` and the CCW x-aligned circle property candidate with
radius `5`, center `(1,1)`, area `PI * 25`, path length `2 * PI * 5`, and
extents `(-4,-4)-(6,6)` only when they pass. If any candidate fails current
Rust behavior, keep the suite green by converting that candidate into a
metadata-only `Gap` record with the same fixture id and a reason naming the
observed mismatch.
</action>
<verify>
`cargo test -p cavalier_contours --test test_historical_cavalier_contours -- --nocapture`
</verify>
<acceptance_criteria>
- `test_historical_cavalier_contours.rs` contains the exact old C++ commit hash.
- At least one offset fixture, one boolean or combine fixture, and one property fixture are present as either executable or metadata-only records.
- At least one historical fixture executes through `run_fixture`.
- Any failing candidate is metadata-only and does not make the targeted test red.
</acceptance_criteria>
<done>
Curated old C++ evidence is represented in typed Rust fixtures with provenance.
</done>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_fixture_harness`
- `cargo test -p cavalier_contours --test test_historical_cavalier_contours -- --nocapture`
- `cargo fmt --all --check`
- `git diff --check`

</verification>

<success_criteria>

- Historical executable fixtures run through `run_fixture(&FixtureCase)`.
- C API and spatial-index records are metadata-only when they cannot execute.
- Test-only schema/harness changes remain under `cavalier_contours/tests/`.
- No public Rust API, FFI implementation, or generated header changes occur.

</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-c-evidence-mining/03-02-SUMMARY.md`.
</output>
