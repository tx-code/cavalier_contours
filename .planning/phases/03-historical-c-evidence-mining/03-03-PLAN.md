---
phase: 03
plan: 03-03
type: implementation
wave: 3
depends_on:
  - 03-02
autonomous: true
requirements:
  - FIX-03
files_modified:
  - cavalier_contours/tests/test_historical_cavalier_contours.rs
  - .planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md
must_haves:
  truths:
    - "D-02: C API evidence remains metadata-only and migration-sensitive."
    - "D-03: Static spatial index evidence remains inventory/metadata and does not become benchmark work."
    - "D-06: Imported executable cases run through Phase 2 run_fixture harness."
    - "D-08: cargo test remains green for executable imported fixtures."
    - "D-09: Mismatches are metadata-only gaps, not red tests."
    - "D-11: Metadata coverage proves source repo, commit, path, usage label, comparison, and executable status are visible."
    - "D-12: Validation checks property parity instead of literal vertex ordering."
  artifacts:
    - "cavalier_contours/tests/test_historical_cavalier_contours.rs"
    - ".planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md"
  key_links:
    - "The historical fixture test verifies executable fixtures and non-executable metadata records."
    - "03-INVENTORY.md reflects final executable versus metadata-only status after targeted tests."
---

# Plan 03-03: Validate Imported Historical Evidence

<objective>
Add the validation layer that proves Phase 3 imported fixtures are executable
through the Phase 2 harness, metadata-only records remain non-executable, and
the inventory reflects the final status of each historical evidence item.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-historical-c-evidence-mining/03-CONTEXT.md
@.planning/phases/03-historical-c-evidence-mining/03-RESEARCH.md
@.planning/phases/03-historical-c-evidence-mining/03-VALIDATION.md
@.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md
@cavalier_contours/tests/test_historical_cavalier_contours.rs
</context>

<threat_model>

- **T-03-07: Inventory and tests drift apart.**
  Mitigation: validation asserts that final fixture IDs and metadata records are
  represented in both the test and `03-INVENTORY.md`.
- **T-03-08: Metadata-only records accidentally execute.**
  Mitigation: targeted tests must assert `executed == false` for C API,
  spatial-index, and gap records.
- **T-03-09: Narrow targeted tests pass while workspace breaks.**
  Mitigation: final verification includes targeted harness tests, workspace
  tests, formatting, clippy, and diff whitespace checks.

</threat_model>

<tasks>

<task>
<name>Add fixture execution and metadata coverage assertions</name>
<files>
- `cavalier_contours/tests/test_historical_cavalier_contours.rs`
</files>
<read_first>
- `cavalier_contours/tests/test_historical_cavalier_contours.rs`
- `cavalier_contours/tests/test_fixture_harness.rs`
- `cavalier_contours/tests/test_utils/fixture_schema.rs`
- `cavalier_contours/tests/test_utils/fixture_harness.rs`
</read_first>
<action>
Update the historical fixture test so it has one collection function for
executable fixtures and one collection function for metadata-only records. Add
tests that call `run_fixture` for every executable fixture, assert each summary
has `executed == true`, and assert metadata-only records for
`historical-cpp-c-api-surface-migration-record`,
`historical-cpp-static-spatial-index-query-record`, and any gap fallback have
`executed == false`. Use `fixture_metadata` to assert source path, usage label,
comparison mode, operation kind, and executable status are observable.
</action>
<verify>
`cargo test -p cavalier_contours --test test_historical_cavalier_contours -- --nocapture`
</verify>
<acceptance_criteria>
- At least one historical fixture executes and at least one metadata-only record does not execute.
- C API and static spatial-index record IDs are asserted by name.
- Metadata assertions include usage label, comparison mode, operation kind, and executable status.
</acceptance_criteria>
<done>
The historical fixture test proves both executable and metadata-only evidence paths.
</done>
</task>

<task>
<name>Synchronize inventory final status</name>
<files>
- `.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md`
</files>
<read_first>
- `.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md`
- `cavalier_contours/tests/test_historical_cavalier_contours.rs`
</read_first>
<action>
Update `03-INVENTORY.md` so each selected fixture row has a final status of
`executable-green`, `metadata-only-gap`, `metadata-only-not-comparable`, or
`deferred`. Add a `Harness Validation` section naming the targeted test command
`cargo test -p cavalier_contours --test test_historical_cavalier_contours` and
the workspace gate `cargo test --workspace`. Ensure C API and spatial-index
records remain non-executable.
</action>
<verify>
`Select-String -Path .planning\phases\03-historical-c-evidence-mining\03-INVENTORY.md -Pattern "Harness Validation","executable-green","metadata-only","cargo test -p cavalier_contours --test test_historical_cavalier_contours"`
</verify>
<acceptance_criteria>
- `03-INVENTORY.md` lists final status for every selected fixture and metadata record.
- `03-INVENTORY.md` names the targeted historical fixture test command.
- C API and spatial index records are not marked executable.
</acceptance_criteria>
<done>
The planning inventory matches the actual test evidence created in Phase 3.
</done>
</task>

<task>
<name>Run final Phase 3 verification gate</name>
<files>
- `cavalier_contours/tests/test_historical_cavalier_contours.rs`
- `.planning/phases/03-historical-c-evidence-mining/03-INVENTORY.md`
</files>
<read_first>
- `.planning/phases/03-historical-c-evidence-mining/03-VALIDATION.md`
- `AGENTS.md`
</read_first>
<action>
Run the Phase 3 validation commands and address only narrow test/documentation
issues inside the files owned by this phase. Required commands are
`cargo test -p cavalier_contours --test test_historical_cavalier_contours`,
`cargo test -p cavalier_contours --test test_fixture_harness`,
`cargo test --workspace`, `cargo fmt --all --check`,
`cargo clippy --all-targets -- -D warnings`, and `git diff --check`. Do not
touch public Rust API files, FFI files, generated headers, benchmarks, or UI.
</action>
<verify>
`cargo test --workspace`
</verify>
<acceptance_criteria>
- Targeted historical fixture test passes.
- Phase 2 fixture harness test still passes.
- Workspace tests pass.
- Formatting, clippy, and diff whitespace gates pass.
- `git diff --name-only -- cavalier_contours/src cavalier_contours_ffi cavalier_contours_ffi.h` returns no Phase 3 implementation changes.
</acceptance_criteria>
<done>
Phase 3 evidence is validated and ready for phase verification.
</done>
</task>

</tasks>

<verification>

- `cargo test -p cavalier_contours --test test_historical_cavalier_contours -- --nocapture`
- `cargo test -p cavalier_contours --test test_fixture_harness`
- `cargo test --workspace`
- `cargo fmt --all --check`
- `cargo clippy --all-targets -- -D warnings`
- `git diff --check`
- `gsd-sdk query check.decision-coverage-plan .planning/phases/03-historical-c-evidence-mining .planning/phases/03-historical-c-evidence-mining/03-CONTEXT.md`

</verification>

<success_criteria>

- Imported executable cases run through the Phase 2 harness.
- Metadata-only C API, spatial-index, and gap records are present and do not
  execute behavior assertions.
- `03-INVENTORY.md` and the historical fixture test agree on final evidence
  status.
- Workspace verification is green with no FFI/header/UI scope drift.

</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-c-evidence-mining/03-03-SUMMARY.md`.
</output>
